import Section from '../../components/Section'
import { RefProps } from '../../global'

export default function Tecnologies({ innerRef }: RefProps): JSX.Element {
    return (
        <Section title="Tecnologías" innerRef={innerRef}>
            Tecnologies
        </Section>
    )
}
