import Section from '../../components/Section'
import type { RefProps } from '../../global'

export default function Experience({ innerRef }: RefProps): JSX.Element {
    return (
        <Section title="Experiencia" innerRef={innerRef}>
            Experience
        </Section>
    )
}
